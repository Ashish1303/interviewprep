import { extendedReactInterviewQuestions } from './reactInterviewQuestionsExtended';

export type ReactInterviewQuestion = {
  id: number;
  question: string;
  answer: string;
  topic?: string;
  example?: string | { jsx?: string; javascript?: string };
  examples?: Record<string, string>;
  benefits?: string[];
  downsides?: string[];
  keyPoints?: string[];
  modernNote?: string;
};

export const reactInterviewQuestions: ReactInterviewQuestion[] = [
  { id: 1, question: 'What is React, and what are its main features?', answer: 'React is a JavaScript library developed by Facebook for creating user interfaces, particularly in single-page applications. It enables reusable components that manage their own state. Key advantages include component-driven architecture, optimized updates through the virtual DOM, a declarative approach, and strong community support.', topic: 'React Features' },
  { id: 2, question: 'What is JSX and how does it work?', answer: 'JSX is a syntax extension for JavaScript that lets you write HTML-like code within JavaScript. It makes React components easier to build and is compiled into JavaScript function calls, often by Babel.', topic: 'How JSX works', example: { jsx: '<div>Hello, world!</div>', javascript: "React.createElement('div', null, 'Hello, world!')" } },
  { id: 3, question: 'Explain the concept of the Virtual DOM in React.', answer: 'The virtual DOM is an in-memory representation of the actual DOM. React compares a new virtual DOM tree with the previous one and applies only the necessary changes through reconciliation.' },
  { id: 4, question: 'How does virtual DOM in React work? What are its benefits and downsides?', answer: 'When state or props change, React creates a new virtual DOM tree, compares it with the previous tree using a diffing algorithm, and updates only the changed parts of the real DOM.', benefits: ['Improves performance by reducing costly direct DOM manipulations', 'Makes UI updates declarative and predictable'], downsides: ['Diffing adds some overhead', 'It requires extra memory', 'It may not outperform manual optimization in every dynamic UI'] },
  { id: 5, question: 'What is the difference between React Node, React Element, and React Component?', answer: 'A React Node is anything React can render, including an element, string, number, or null. A React Element is an immutable object describing what should be rendered. A React Component is a function or class that returns React Elements and enables reusable UI.' },
  { id: 6, question: 'What are React Fragments used for?', answer: "React Fragments group multiple elements without adding an extra DOM node. They are useful when a component must return siblings without wrapping them in a container. Use shorthand <>...</> or React.Fragment.", example: 'return (\n  <>\n    <ChildComponent1 />\n    <ChildComponent2 />\n  </>\n);' },
  { id: 7, question: 'What is the purpose of the key prop in React?', answer: 'The key prop uniquely identifies elements in a list, allowing React to update and reorder items efficiently. Keys should be stable and tied to the item identity.' , example: '{items.map((item) => <ListItem key={item.id} value={item.value} />)}' },
  { id: 8, question: 'What is the consequence of using array indices as keys in React?', answer: 'Array indices can cause performance issues and incorrect UI state when items are reordered, inserted, or deleted because the key no longer represents the same item.' },
  { id: 9, question: 'What are props in React? How are they different from state?', answer: 'Props are immutable inputs passed from a parent to a child to configure a component. State is internal component data that can change over time, typically in response to events or other updates.' },
  { id: 10, question: "What is the difference between React's class components and functional components?", answer: 'Class components extend React.Component and use lifecycle methods and this.state. Function components are plain functions that use hooks such as useState, useEffect, and useRef. Function components are the default for new code; class components remain mainly for legacy code and error boundaries.' },
  { id: 11, question: 'When should you use a class component over a function component?', answer: 'Default to function components. The main remaining reason to write a class is an error boundary using static getDerivedStateFromError or componentDidCatch, since that API still requires a class.' },
  { id: 12, question: 'What is React Fiber?', answer: 'React Fiber is a rewrite of React core that breaks rendering into smaller units of work. It enables React to pause, prioritize, or abandon rendering work and supports features such as incremental rendering and error boundaries.' },
  { id: 13, question: 'What is reconciliation?', answer: 'Reconciliation is how React compares a new virtual DOM tree with the previous tree and determines the minimum changes needed to update the real DOM efficiently.' , topic: 'React Reconciliation' },
  { id: 14, question: 'What is the difference between Shadow DOM and Virtual DOM?', answer: 'Shadow DOM is a web platform feature that encapsulates DOM and styles. Virtual DOM is React\'s in-memory UI representation used to calculate efficient updates; they solve different problems.' },
  { id: 15, question: 'What is the difference between Controlled and Uncontrolled React components?', answer: 'A controlled component stores form data in React state, making state the source of truth. An uncontrolled component stores form data in the DOM and is accessed with a ref. Controlled components offer more control and testability; uncontrolled components can be simpler for basic forms.', examples: { controlled: "function ControlledInput() {\n  const [value, setValue] = React.useState('');\n  return <input value={value} onChange={(e) => setValue(e.target.value)} />;\n}", uncontrolled: 'function UncontrolledInput() {\n  const inputRef = React.useRef();\n  return <input ref={inputRef} />;\n}' } },
  { id: 16, question: 'How would you lift the state up in a React application, and why is it necessary?', answer: 'Lifting state up means moving state to the nearest common ancestor of the components that need it. The parent owns the value and passes data and event handlers down, creating one source of truth and avoiding duplicated state.', example: 'const Parent = () => {\n  const [counter, setCounter] = useState(0);\n  return <><Child1 counter={counter} /><Child2 setCounter={setCounter} /></>;\n};' },
  { id: 17, question: 'What are Pure Components?', answer: 'Pure components re-render only when their props or state change according to a shallow comparison. Class components can extend React.PureComponent and function components can use React.memo. With the React Compiler, manual memoization is less often needed.', example: 'const PureExample = React.memo(function ({ value }) {\n  return <div>{value}</div>;\n});' },
  { id: 18, question: 'What is the difference between createElement and cloneElement?', answer: 'createElement creates a new React element from a type, props, and children. cloneElement copies an existing element and optionally overrides its props while preserving its children.', example: "const element = <button className=\"btn\">Click Me</button>;\nconst cloned = React.cloneElement(element, { className: 'btn-primary' });" },
  { id: 19, question: 'What is the role of PropTypes in React?', answer: 'PropTypes was React\'s development-time runtime prop checker. It warned when values did not match declared types. It is deprecated as of React 19, so TypeScript is the preferred option for new code.', example: 'MyComponent.propTypes = { name: PropTypes.string.isRequired };' },
  { id: 20, question: 'What are stateless components?', answer: 'Stateless components do not manage internal state. They receive data through props and render UI from that data, making them focused, reusable, and easy to test.', keyPoints: ['Do not use this.state', 'Render UI based on props', 'Focus on presenting information'], example: 'function StatelessComponent({ message }) {\n  return <div>{message}</div>;\n}' },
  { id: 21, question: 'What are stateful components?', answer: 'Stateful components manage internal state and update it in response to user interactions or other events. Modern stateful components are usually functions using hooks such as useState.', keyPoints: ['Use state to model changing data', 'Update state through event handlers or actions', 'Own behavior and data management'], example: 'function StatefulComponent() {\n  const [count, setCount] = React.useState(0);\n  return <button onClick={() => setCount(count + 1)}>{count}</button>;\n}' },
  { id: 22, question: 'What are the recommended ways for type checking of React component props?', answer: 'Use TypeScript. It catches prop mismatches at compile time, integrates with autocomplete and refactoring tools, and is the default in most modern React projects. PropTypes is the older runtime-checking alternative.', example: 'type Props = { name: string; age: number };\nfunction Profile({ name, age }: Props) { return <div>{name}: {age}</div>; }' },
  { id: 23, question: 'Why does React recommend against mutating state?', answer: 'Direct state mutation can produce unexpected behavior because React relies on new references and state updates to detect changes. Immutable updates make change detection predictable and preserve reliable re-rendering.' },
  ...extendedReactInterviewQuestions,
];
