import type { SectionTopic } from './sectionTypes';

export const nodeTopics: SectionTopic[] = [
  { slug: 'node-event-loop', title: 'Event Loop', summary: 'Node.js uses an event loop and non-blocking I/O to handle many concurrent requests efficiently.', whyItMatters: ['Blocking work stalls every request on a process.', 'I/O is delegated while callbacks wait for results.', 'Event-loop knowledge explains latency and concurrency.'], questions: ['How does Node handle concurrency?', 'Why is synchronous code dangerous in an API?', 'How do you identify event-loop blocking?'], code: "server.get('/health', async (_request, response) => response.json({ ok: true }));", commonMistakes: ['Doing CPU-heavy work in request handlers.', 'Assuming async makes CPU work non-blocking.', 'Ignoring event-loop latency metrics.'], seniorTips: ['Separate I/O concurrency from CPU parallelism.', 'Move expensive work to workers or queues.'], useCase: 'An API serves many I/O-bound requests while database calls are in flight.' },
  { slug: 'node-api-design', title: 'API Design', summary: 'Production Node APIs need clear boundaries, validation, consistent errors, and observable request flows.', whyItMatters: ['Middleware keeps cross-cutting behavior consistent.', 'Validation protects business logic and data stores.', 'Stable errors make clients easier to maintain.'], questions: ['How do you structure an Express API?', 'What belongs in middleware?', 'How do you validate a request payload?'], code: "router.post('/users', validate(createUserSchema), createUser);", commonMistakes: ['Putting all logic in route handlers.', 'Trusting client-provided data.', 'Returning inconsistent error shapes.'], seniorTips: ['Keep routes thin and services testable.', 'Document status codes and failure contracts.'], useCase: 'A service validates requests at the edge and delegates business rules to isolated modules.' },
  { slug: 'node-streams-and-buffers', title: 'Streams & Buffers', summary: 'Streams process large data incrementally, while buffers represent raw binary data for files and network payloads.', whyItMatters: ['Streaming controls memory use for large payloads.', 'Transform streams build composable data pipelines.', 'Buffers make binary I/O explicit.'], questions: ['Why are streams useful for uploads?', 'What is the difference between a stream and a buffer?', 'When would you use a transform stream?'], code: "request.pipe(createWriteStream('upload.bin'));", commonMistakes: ['Loading an entire file into memory.', 'Ignoring backpressure.', 'Forgetting stream error handling.'], seniorTips: ['Explain backpressure as producer-consumer coordination.', 'Test interrupted and partial transfers.'], useCase: 'A file service uploads and transforms large media without buffering it all in RAM.' },
  { slug: 'node-security', title: 'Security', summary: 'Secure Node services validate input, protect secrets, enforce authorization, and control dependencies.', whyItMatters: ['Server-side checks are authoritative.', 'Dependency and secret hygiene reduce attack surface.', 'AuthN and AuthZ are separate responsibilities.'], questions: ['How do you prevent unsafe input handling?', 'Where should authorization happen?', 'How do you protect secrets in production?'], code: "if (!user.can('invoice:read')) return response.sendStatus(403);", commonMistakes: ['Relying on client-side authorization.', 'Logging credentials or tokens.', 'Skipping dependency audits.'], seniorTips: ['Threat-model sensitive routes.', 'Fail closed and make security behavior observable.'], useCase: 'An admin API checks identity and permissions before every sensitive mutation.' },
  { slug: 'node-scaling-and-performance', title: 'Scaling & Performance', summary: 'Node services scale through efficient I/O, process boundaries, caching, queues, and measured database access.', whyItMatters: ['CPU-heavy work needs a different execution strategy.', 'Queues smooth bursts and isolate slow jobs.', 'Latency and throughput should guide scaling decisions.'], questions: ['When should you use worker threads?', 'How do queues improve reliability?', 'What should you monitor in production?'], code: "await queue.add('generate-report', { reportId });", commonMistakes: ['Adding replicas before fixing slow queries.', 'Doing long jobs synchronously.', 'Monitoring only average latency.'], seniorTips: ['Track p95 and p99 latency.', 'Scale the bottleneck, not the whole system by reflex.'], useCase: 'A report-generation queue keeps API requests fast while workers handle expensive jobs.' },
];
